# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/prime_property

# App
PORT=4000
NODE_ENV=development
FRONTEND_ORIGIN=http://localhost:5173
NEXT_PUBLIC_BASE_URL=http://localhost:5173

# Session
SESSION_SECRET=replace_this_with_secure_random_value
COOKIE_DOMAIN=localhost

# Email (Contact Form)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=replace_this
SMTP_FROM=no-reply@primeproperty.id
ADMIN_EMAIL=admin@primeproperty.id
